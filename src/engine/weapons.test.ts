import { describe, it, expect } from 'vitest';
import { WEAPON_CONFIGS } from './weapons.ts';

describe('weapons', () => {
  it('placeholder', () => {
    expect(WEAPON_CONFIGS).toBeDefined();
  });
});
